import React from 'react';

const Home = () => {
    return (
        <div>
            <div> hello world </div>
            <button onClick={() => console.log('hi there!')}>Press me!</button>
        </div>
    )
}

export default Home;